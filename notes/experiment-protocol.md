# Experiment protocol (standard for every run in this fork)

Purpose: every experiment is executed and recorded the same way, so that any table in the
paper can be traced to one folder whose report.pdf states the policy choice, every
parameter, the statistics and the raw files. Nothing is run by hand.

House style: no em dashes or en dashes.

## 1. One experiment = one folder

`ours/experiments/<date>_<name>/`, created by `python ours/sweep.py <name> ...`. Contents:

| File | Role |
|---|---|
| `manifest.json` | machine record: fork commit, profile, every BLIS flag, rate grid, seeds, horizon, warm-up, tail, sampling intervals, spec template |
| `specs/rate<R>.yaml` | the exact workload file BLIS read for that rate |
| `runs/rate<R>_s<S>.json.gz`, `.log`, `_state.csv`, `_snapshot.csv`, `_panel.png` | raw output per sample path, plus its six-panel figure |
| `summary_runs.csv`, `summary.csv` | per-path window metrics; across-seed mean and CI |
| `objective_runs.csv`, `objective.csv`, `objective.png` | revenue management objective (once prices are set) |
| `README.md` | text only: the question (before the first "##") and the interpretation (after) |
| `report.pdf` | generated by `python ours/report.py <folder>`: the e-companion numerical details |

## 2. What is fixed and what varies

Fixed for the whole campaign (BASE_FLAGS in sweep.py): Qwen3-14B, H100, TP 1, 4 instances,
`ours/specs/types3.yaml` (three Poisson types, shares 0.5 / 0.35 / 0.15).

Chosen once per experiment and written into the manifest:

| Decision | Options | Recorded as |
|---|---|---|
| Policy profile | B1 (llm-d default, the anchor), B0 (vLLM alone), floor, oracle, harness, or a named candidate added to PROFILES | `--profile` |
| Memory regime | 15,909 blocks (auto pool, validation) or 2,500 blocks (memory-bound, our target) | `--blocks` |
| Load | total arrival rate grid | `--rates` |
| Sample paths | seed list, 20 by default, identical across compared policies (common random numbers) | `--seeds` |
| Horizon and window | `--horizon-s T --warmup-s W --tail-s D`; near capacity T 1200, W 500, D 100 | manifest |
| Sampling | `--state-sample-ms` (counts per instance), `--state-snapshot-s` (queue and batch composition) | manifest |

Rule: consecutive rows of any comparison table differ in exactly one decision, and the B1
row comes first.

## 3. Statistics (ours/analyze.py, ours/objective.py)

- Unit of observation: one sample path (one seed). Every statistic is computed per path
  first; per-request records within a path are not treated as independent.
- Steady-state window: requests arriving in [W, T - D). Latency statistics use completed
  window requests. Unfinished-at-horizon requests are counted (fraction), never timed.
  Rejected requests are counted per type.
- Reported per path and type: mean, p50, p99 of TTFT, E2E and queue wait; unfinished and
  rejected fractions; preemptions and wasted tokens per arrival; output tokens per second.
- Objective: time average over the window, value per second = reward per second minus
  congestion cost per second, prices from `ours/specs/objective.yaml`.
- Across paths: mean and 95 percent Student t half-width over the K seeds. A ranking
  between policies is claimed only if it holds on every seed.

## 4. Execution checklist

1. State the question in one sentence (`--question`); it becomes section 1 of the PDF.
2. Pick the profile, memory regime, grid, seeds, horizon and window from the table above.
3. Run `python ours/sweep.py ...`. It writes the manifest, runs BLIS, gzips the json,
   runs analyze.py.
4. If prices are set, run `python ours/objective.py <folder>`.
5. Write the interpretation into README.md under a "## Interpretation" heading, then
   `python ours/report.py <folder>` to produce report.pdf.
6. If the experiment is superseded, say so at the top of README.md and point to the folder
   that replaces it. Delete folders that were mechanism checks once they served their purpose.
7. Commit code changes with a message starting `ours:` (experiment folders are gitignored).

## 5. Sanity anchors (notes/benchmark-brief.md section 4)

At low load: ITL mean 10 to 20 ms, TTFT for short prompts under 100 ms;
`dropped_unservable` and `length_capped_requests` zero. A run off by more than 2x is a
configuration error, not a result.
